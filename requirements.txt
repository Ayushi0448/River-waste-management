# ── Core web app + trained Roboflow detector (minimum to run) ──
flask==3.1.1
flask-cors==6.0.5
flask-sqlalchemy==3.1.1
opencv-python==4.13.0.92
numpy==2.4.6
groq==1.4.0
roboflow==1.3.10
python-dotenv==1.2.2

# ── Object-detection ensemble (COCO-YOLOv5 path) ──
# Detects bottles/cups in real-world photos that the narrow Roboflow model
# misses. The app still runs without these (it falls back to the Roboflow
# model + the built-in CV litter detector), but install them for best recall.
# NOTE: torch/torchvision come from the PyTorch CPU index. Install with:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#   pip install ultralytics pandas seaborn
torch==2.12.0
torchvision==0.27.0
ultralytics==8.4.67
pandas==3.0.3
seaborn==0.13.2
